import styled from 'styled-components';
// import { Nav, NavItem, NavProps } from "./Nav"
// import { LogoIcon, IconProps } from "../Link"

// export type HeaderProps = {
//   icon: IconProps
//   navs: NavProps[]
// }

// export const Header = (props: HeaderProps) => (
//   <header>
//     <LogoIcon href={props.icon.href} className={props.icon.className}>
//         {props.icon.children}
//     </LogoIcon>
//     <Nav>
//       {props.navs.forEach(nav => (
//         <NavItem name={nav.name} href={nav.href} className={nav.className} />
//       ))}
//     </Nav>
//   </header>
// )